<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Internal app files -->
    <files-path name="internal" path="." />

    <!-- External app-specific files -->
    <external-files-path name="exports" path="Exports/" />
    <external-files-path name="timelapse" path="Timelapse/" />
    <external-files-path name="crash_reports" path="crash_reports/" />

    <!-- Shared external storage -->
    <external-path name="shared" path="." />

    <!-- Cache for temporary files -->
    <cache-path name="cache" path="." />
</paths>
